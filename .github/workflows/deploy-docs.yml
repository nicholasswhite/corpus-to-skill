name: Deploy docs

on:
  push:
    branches: [master]
    paths:
      - docs/**
      - README.md
      - SKILL.md
      - BACKERS.md
      - mkdocs.yml
      - .github/workflows/deploy-docs.yml
  pull_request:
    paths:
      - docs/**
      - README.md
      - SKILL.md
      - BACKERS.md
      - mkdocs.yml
      - .github/workflows/deploy-docs.yml

# Minimal: gh-deploy pushes the built site to the gh-pages branch.
permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      # Actions pinned to commit SHAs (tags in comments) so a supply-chain
      # swap on a moving tag cannot alter the build. Zizmor-clean.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install MkDocs Material
        run: pip install mkdocs-material
      - name: Assemble docs sources
        # Single-source: the Guide and Skill Reference pages are the repo-root
        # README.md / SKILL.md, copied in (never committed under docs/). The
        # landing page docs/index.md is committed and curated separately.
        run: |
          cp README.md docs/guide.md
          cp SKILL.md docs/skill-reference.md
          cp BACKERS.md docs/BACKERS.md
          sed -i \
            -e 's#](SKILL.md)#](skill-reference.md)#g' \
            -e 's#](docs/#](#g' \
            -e 's#](CONTRIBUTING.md)#](https://github.com/nicholasswhite/corpus-to-skill/blob/master/CONTRIBUTING.md)#g' \
            -e 's#](SECURITY.md)#](https://github.com/nicholasswhite/corpus-to-skill/blob/master/SECURITY.md)#g' \
            -e 's#](LICENSE.md)#](https://github.com/nicholasswhite/corpus-to-skill/blob/master/LICENSE.md)#g' \
            docs/guide.md
          sed -i 's#](../README.md)#](guide.md)#g' \
            docs/FAQ.md docs/HOW_IT_WORKS.md docs/INSTALL.md docs/USAGE.md
      - name: Build
        run: mkdocs build
      - name: Deploy to gh-pages (master push only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        run: mkdocs gh-deploy --force
